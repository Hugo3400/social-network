import React from 'react';
import { Container, Typography } from '@mui/material';

export default function Groups() {
  return (
    <Container maxWidth="md" sx={{ py: 4 }}>
      <Typography variant="h4">Groups & Communities</Typography>
      <Typography color="text.secondary">Groups page coming soon...</Typography>
    </Container>
  );
}
